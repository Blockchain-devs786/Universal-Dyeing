import PlaceholderPage from "@/components/PlaceholderPage";
import { Users } from "lucide-react";
export default function UserManagement() {
  return <PlaceholderPage title="User Management" description="Manage user accounts, roles and permissions for your organization." icon={Users} />;
}
